# Rust build artifacts
/target/
**/target/

# Claude Code / local tooling metadata
/.claude/

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Code-signing material — never commit private keys (see
# apps/oxidraft_app/assets/PACKAGING.md; the real .pfx lives outside the repo)
*.pfx
*.p12

# Illustrator scratch and recovery files. Tilde-prefixed, PDF-based, and they
# land beside the .ai they came from at nearly the same size — which makes them
# easy to sweep up with a broad `git add`, and they are not source.
~*.tmp
